package com.example.connectue.interfaces;

/**
 * Callback for an asynchronous method that check that a user is verified.
 */
public interface VerificationCallback {
    void onVerificationCallback(boolean isVerified);
}
